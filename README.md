# GG CODE

<div align="center">

```
██╗   ██╗███████╗██████╗ ███████╗
██║   ██║██╔════╝██╔══██╗██╔════╝
██║   ██║█████╗  ██║  ██║█████╗
╚██╗ ██╔╝██╔══╝  ██║  ██║██╔══╝
 ╚████╔╝ ███████╗██████╔╝███████╗
  ╚═══╝  ╚══════╝╚═════╝ ╚══════╝

  ███╗   ███╗ ██████╗ ███╗   ██╗
  ████╗ ████║██╔═══██╗████╗  ██║
  ██╔████╔██║██║   ██║██╔██╗ ██║
  ██║╚██╔╝██║██║   ██║██║╚██╗██║
  ██║ ╚═╝ ██║╚██████╔╝██║ ╚████║
  ╚═╝     ╚═╝ ╚═════╝ ╚═╝  ╚═══╝
```

**AI-Powered Code Editor CLI Tool**

基于内网聊天API的代码编辑CLI工具，支持交互式对话、AI自主编程、代码编辑和搜索功能。

</div>

## ✨ 功能特性

- **🤖 AI自主编程**: 类似Claude Code，AI可以自主执行文件操作、命令执行等任务
- **💬 交互式对话**: 与AI助手进行多轮对话
- **📁 文件上下文**: 自动加载和分析代码文件
- **✏️ 代码编辑**: 智能识别并应用代码修改
- **🔍 代码搜索**: 快速搜索代码模式
- **📜 会话管理**: 多会话支持，方便切换不同任务
- **⚙️ 配置管理**: 灵活的配置系统
- **⌨️ 快捷键支持**: P键中断，Ctrl+C退出

## 📦 安装

### 环境要求

- Node.js >= 16.0.0
- npm (或内网npm仓库)

### 安装依赖

```bash
npm install
```

### 编译项目

```bash
npm run build
```

## 🚀 快速开始

### 启动 GG CODE

```bash
npm run agent
```

**启动选项：**
```bash
npm run agent                    # 启动 AI 编程助手
npm run agent -- -y              # 自动批准所有工具调用
npm run agent -- -a explore       # 使用 explore agent (只读模式)
npm run agent -- -a build         # 使用 build agent (构建专家)
```

### 配置说明

配置文件位于 `~/.ggcode/config.json`，首次运行会自动创建默认配置。

**主要配置项：**
- `api.base_url` - API 基础 URL
- `api.model` - 使用的模型名称
- `agent.max_context_tokens` - 最大上下文 tokens (默认 8000)
- `agent.max_history` - 最大历史轮数 (默认 20)
- `agent.max_iterations` - 最大迭代次数 (默认 10)
- `agent.auto_approve` - 自动批准工具调用 (默认 false)

## 💡 使用技巧

### 快捷键

- **P 键**: 中断当前操作（AI思考或工具执行中）
- **Ctrl+C**: 退出程序

### 内置命令

在对话中输入 `/` 查看所有可用命令：

- `/init` - 创建或更新项目文档 (AGENTS.md)
- `/models [模型名]` - 查看或切换模型
- `/session new/list/switch/[id]` - 会话管理
- `/compress on/off/manual/status` - 上下文压缩控制
- `/tokens` - 显示 token 使用统计
- `/test` - 测试交互式选择功能
- `exit` 或 `clear` - 退出或清空历史

## 🛠️ 开发

### 常用命令

```bash
# 编译
npm run build                # 编译 TypeScript

# 类型检查
npm run typecheck            # 类型检查（不生成文件）

# AI 编程助手
npm run agent                # 启动 AI 编程助手

# 测试
npm test                     # 运行 Jest 测试
npm run test:watch           # 监视模式
npm run test:coverage        # 生成覆盖率报告
npm run test:tools           # 工具系统快速测试

# 代码质量
npm run lint                 # ESLint 检查
npm run lint:fix             # ESLint 自动修复
npm run format               # Prettier 格式化
npm run format:check         # 检查格式

# 清理
npm run clean                 # 清理所有临时文件
npm run clean:dist           # 只清理 dist
```

### 项目结构

```
├── src/                      # 源代码
│   ├── api/                  # API 适配器
│   ├── commands/             # CLI 命令
│   ├── config/               # 配置管理
│   ├── core/                 # 核心功能
│   │   ├── agent.ts          # Agent 编排器
│   │   ├── tool-engine.ts    # 工具引擎
│   │   ├── context-manager.ts # 上下文管理
│   │   └── session-manager.ts # 会话管理
│   ├── tools/                # 工具定义
│   │   └── prompts/          # Agent 系统提示词
│   │       ├── default.txt   # 默认 agent
│   │       ├── explore.txt   # explore agent
│   │       ├── tools/        # 工具使用说明
│   │       └── ...
│   ├── types/                # 类型定义
│   └── utils/                # 工具函数
├── src/prompts/              # 项目提示词模板
│   └── init.txt              # 初始化模板
├── tests/                   # 测试文件
├── scripts/                 # 构建脚本
├── bin/                     # 可执行文件
├── config/                  # 配置文件示例
└── package.json
```

## 📚 文档

- **CLAUDE.md** - Claude Code 使用指南
- **DESIGN.md** - 设计文档
- **tests/README.md** - 测试文档

## 🔧 工具系统

项目实现了完整的工具系统，包含以下工具：

**文件操作**
- `Read` - 读取文件内容
- `Write` - 写入文件
- `Edit` - 编辑文件（字符串替换）
- `MultiEdit` - 多次编辑同一文件

**搜索工具**
- `Glob` - 文件名模式搜索
- `Grep` - 文件内容正则搜索

**执行工具**
- `Bash` - 执行 shell 命令

**高级功能**
- `Task` - 启动专门的 agent（explore 等）
- `Batch` - 批量并行执行工具
- `TodoWrite/Read/Delete/Clear` - 任务管理

## 🐛 常见问题

### 1. API调用失败

检查：
- 网络连接是否正常
- API配置是否正确（`~/.ggcode/config.json`）
- 认证信息是否有效

### 2. 编译错误

```bash
# 清理并重新编译
npm run clean:dist
npm run build
```

### 3. 工具执行失败

- 检查文件路径是否正确
- 确认工具权限配置
- 查看错误日志

## 📄 License

MIT
