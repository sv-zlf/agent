# 安装指南

本指南介绍如何安装和使用 GG CODE 命令行工具。

## 📦 安装方式

### 方式 1：本地开发安装（推荐）

在项目根目录执行：

```bash
# 1. 安装依赖
npm install

# 2. 编译项目
npm run build

# 3. 全局链接命令
npm link
```

安装完成后，你可以在任何目录使用 `ggcode` 命令：

```bash
ggcode --version
ggcode --help
ggcode                    # 启动 AI 编程助手
ggcode -y                  # 自动批准所有工具调用
ggcode -a explore           # 使用 explore agent
```

### 方式 2：全局安装

从 npm 或本地包全局安装：

```bash
# 从本地安装
npm install -g .

# 或从 npm 安装（如果已发布）
npm install -g gg-code
```

### 方式 3：使用 npx（无需安装）

```bash
npx gg-code
```

## 🔧 验证安装

安装完成后，验证是否成功：

```bash
# 查看版本
ggcode --version

# 查看帮助
ggcode --help

# 启动助手
ggcode
```

## 🚀 使用示例

```bash
# 启动 AI 编程助手
ggcode

# 自动批准所有工具调用
ggcode -y

# 使用 explore agent（只读模式）
ggcode -a explore

# 使用 build agent（构建专家）
ggcode -a build

# 不保存对话历史
ggcode --no-history

# 指定配置文件
ggcode --config /path/to/config.json
```

## 📋 命令选项

| 选项 | 简写 | 说明 |
|------|------|------|
| `--help` | `-h` | 显示帮助信息 |
| `--version` | `-V` | 显示版本号 |
| `--yes` | `-y` | 自动批准所有工具调用 |
| `--agent <name>` | `-a` | 使用指定的 agent (default/explore/build) |
| `--no-history` | | 不保存对话历史 |
| `--config <path>` | | 指定配置文件路径 |

## 🔗 链接和取消链接

### 链接命令（开发时使用）

```bash
npm run link
```

这会在全局创建一个符号链接，指向当前项目。每次修改代码后需要重新编译：

```bash
npm run build
```

### 取消链接

```bash
npm run unlink
```

或手动取消：

```bash
npm unlink -g ggcode
```

## 🔄 开发工作流

1. **修改代码**
   ```bash
   # 编辑源代码
   vim src/index.ts
   ```

2. **重新编译**
   ```bash
   npm run build
   ```

3. **测试命令**
   ```bash
   ggcode --version
   ```

4. **如果命令不工作**，重新链接：
   ```bash
   npm run unlink
   npm run link
   ```

## 🐛 故障排查

### 问题：命令未找到

**错误信息**：`ggcode: command not found`

**解决方案**：

1. 确认已编译项目：`npm run build`
2. 确认已链接：`npm link`
3. 检查全局 npm bin 目录：
   ```bash
   npm config get prefix
   ```
4. 确保全局 bin 目录在 PATH 中：
   ```bash
   echo $PATH | grep -o "[^:]*npm[^:]*"
   ```

### 问题：权限错误

**错误信息**：`EACCES`

**解决方案**：

使用 sudo（Linux/Mac）或以管理员身份运行（Windows）：
```bash
sudo npm link
```

或修改 npm 全局目录：
```bash
mkdir ~/.npm-global
npm config set prefix '~/.npm-global'
export PATH=~/.npm-global/bin:$PATH
```

### 问题：命令运行失败

**错误信息**：`Cannot find module '../dist/index.js'`

**解决方案**：

确保已编译项目：
```bash
npm run build
```

## 📝 卸载

### 取消本地链接

```bash
npm run unlink
```

### 卸载全局安装

```bash
npm uninstall -g gg-code
```

## 🔄 更新

如果从源码安装：

```bash
# 1. 拉取最新代码
git pull

# 2. 安装依赖
npm install

# 3. 重新编译
npm run build

# 4. 重新链接
npm run unlink
npm run link
```

如果从 npm 安装：

```bash
npm update -g gg-code
```

## 💡 提示

- **开发时使用** `npm link`，可以快速测试更改
- **生产环境使用** `npm install -g .` 进行完整安装
- **每次修改代码后**需要运行 `npm run build` 重新编译
- 使用 `ggcode --help` 查看所有可用选项
